org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.parallel=true
org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8

kotlin.code.style=official

# The head of the version. CI appends the run number through -PVERSION, so two publishes in a row
# cannot produce the same coordinate; a local build and publishToMavenLocal work without it.
sborka.version=0.2.0
