// A build of its own, outside sborka's, for the same reason `parity-probe` is: the question is what
// the toolchain does, and this repository's conventions would be standing between the question and
// the answer.
rootProject.name = "static-probe"
