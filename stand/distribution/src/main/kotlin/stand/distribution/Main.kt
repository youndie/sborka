package stand.distribution

import stand.greeting

/**
 * One line, on purpose: what this module proves is the shape of the distribution — `application`,
 * the start script, one `main` beside a multiplatform module — and not what the program does.
 */
fun main() {
    println(greeting("distribution"))
}
