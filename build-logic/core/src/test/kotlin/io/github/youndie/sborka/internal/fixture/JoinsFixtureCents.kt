package io.github.youndie.sborka.internal.fixture

/** A value class, which is what makes the JVM name of anything mentioning it different. */
@JvmInline
public value class JoinsFixtureCents(
    public val amount: Int,
)
